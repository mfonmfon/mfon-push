import java.util.Scanner;

public class GasMileage{


	public static void main(String[] args){

	Scanner scanner = new scanner(System.in);

	System.out.println("Enter the gallons");
	int gallons = input.nextInt();

	System.out.println("Enter the miles");
	int miles = input.nextInt();

	int gallons;
	int miles; 

	totalGallons = 0;
	totalMiles = 0;


	double milesPerGallons;
	double totalGallonPerMiles;


	while (miles != 1);


	totalMiles += miles;
	totalGallons += gallons;

	
	if (gallons = 0);

	if (milesPerGallons = (double) miles / gallons);{
	System.out.printf("milesPerGallons is tankful %.2f\n", milesPerGallons);
	
	}
	if (totalGallons != 0);
	  totalMilesPerGallons = (double) totalMiles / totalGallons;{
	System.out.printf("Total per gallons is %.2f\n", totalMilesPerGallons);
	}

	System.out.printf("Enter miles (-1 to quit): ");


}


}